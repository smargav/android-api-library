<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smargav.api">

    <application>

        <activity android:name=".logger.LogReaderScreen" />
        <activity android:name=".activities.PermissionsActivity" />
    </application>

</manifest>
